git add .
git commit -m "fix: force update package configuration"
git push origin main


in admin message list. I want it to be similar to the member dashboard one. show: title, last message send and from who. who is the member staff is DMing to in that chat, and also show(unread, unreply, or replied)

add delete button for staff message, same three dots as the member dashboard one

add a delete function for each thing in the staff dashboard that an post(if staff can post anything in their dashboard, their should also be a delete button page for them so they can delete old things, maybe need to write code from supabase sql)